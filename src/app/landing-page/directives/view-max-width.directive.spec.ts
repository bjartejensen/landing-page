import { ViewMaxWidthDirective } from './view-max-width.directive';

describe('ViewMaxWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewMaxWidthDirective();
    expect(directive).toBeTruthy();
  });
});
