import { IntroSectionDirective } from './intro-section.directive';

describe('IntroSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new IntroSectionDirective();
    expect(directive).toBeTruthy();
  });
});
