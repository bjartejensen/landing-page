import { TwoColsImgDirective } from './two-cols-img.directive';

describe('TwoColsImgDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoColsImgDirective();
    expect(directive).toBeTruthy();
  });
});
