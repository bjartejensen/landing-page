import { ApexDimensionsDirective } from './apex-dimensions.directive';

describe('ApexDimensionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ApexDimensionsDirective();
    expect(directive).toBeTruthy();
  });
});
